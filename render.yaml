services:
  - type: web
    name: gcsm-site-api
    env: python
    buildCommand: ""
    startCommand: uvicorn site_api:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
